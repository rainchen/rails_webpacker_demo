{
  "name": "rails-webpacker-demo",
  "description": "dummy package.json for heroku/nodejs buildpack support",
  "private": true,
  "engines": {
    "node": "7.1.0",
    "yarn": "0.18.1"
  },
  "scripts": {
    "heroku-postbuild": "./bin/yarn; ls -l vendor/node_modules"
  }
}
